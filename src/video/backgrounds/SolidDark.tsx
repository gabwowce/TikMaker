import React from "react";
import { colors } from "../typography/tokens";

export const SolidDark: React.FC = () => (
  <div style={{ position: "absolute", inset: 0, backgroundColor: colors.background }} />
);
