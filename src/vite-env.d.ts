/// <reference types="vite/client" />

// Vite's own ambient types — `import.meta.glob`, `import.meta.env`, and the
// module declarations for the asset imports the app already relies on. Added
// when `projectStore` started globbing `projects/*.json` so a fresh clone opens
// with the repo's own library.
